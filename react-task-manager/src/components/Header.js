import React from "react";

// Create a Header functional component.
// Render a single heading component for Task Manager at the top.
const Header = () => {
    return (
        <div className="header">
            <h1>Task Manager App</h1>
        </div>
    );
};

export default Header;